# Leave unset locally so Vite proxies /api/denote -> localhost:5000.
# Production (Vercel): set ONE of these in the host env:
#   VITE_API_BASE_URL=https://denote-yk3d.onrender.com
#   VITE_API_URL=https://denote-yk3d.onrender.com/api/denote
